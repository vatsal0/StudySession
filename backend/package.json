{
  "name": "backend",
  "version": "1.0.0",
  "description": "backend for study session",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon -e js -x node --inspect index.js"
  },
  "author": "vatsal b",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4",
    "socket.io": "^2.2.0"
  }
}
